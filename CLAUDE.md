@AGENTS.md

Static site, no build. Color schemes are locked, shared with `common-action` —
see `AGENTS.md` § Color Schemes (Locked). The `/consult` seam is simulated, not
live — see `AGENTS.md` § The `/consult` seam. Log changes in `MEMORY.md`.
